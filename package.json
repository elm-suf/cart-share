{
  "name": "cart-share",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev:fe",
    "dev:fe": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
